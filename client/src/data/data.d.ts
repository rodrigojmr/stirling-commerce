interface footerNavSectionType {
  title: string;
  links: {
    name: string;
    link: string;
  }[];
}

interface SimpleProduct {
  image: string;
  rating: number;
  title: string;
  price: number;
}
